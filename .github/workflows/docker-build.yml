name: Docker Build Testecho on:echo   push:echo     branches: [ "main" ]echo jobs:echo   build:echo     runs-on: ubuntu-latestecho     steps:echo       - name: Checkout codeecho         uses: actions/checkout@v4echo       - name: Build Docker Imageecho         run: docker build . --file Dockerfile --tag my-flask-app:latest
